{
  "name": "@modulus-learning/root",
  "version": "0.8.0",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "clean": "turbo run clean && rm -rf node_modules",
    "build": "turbo run build",
    "build:packages": "turbo run build --filter=\"./packages/*\"",
    "start": "turbo run start",
    "storybook": "turbo run storybook",
    "dev": "turbo run dev",
    "devlti": "turbo run devlti",
    "devturbo": "turbo run devturbo",
    "dev:seed": "turbo run dev:seed",
    "db:init": "pnpm --filter @modulus-learning/core db:init",
    "db:init:test": "pnpm --filter @modulus-learning/core db:init:test",
    "lint": "turbo run lint --continue",
    "lint:check": "biome check $(find packages/core/src apps/agent/src apps/gradebook/src apps/agent-demo/react/src apps/agent-demo/vanilla/src -type f \\( -name '*.ts' -o -name '*.tsx' \\)) --diagnostic-level=error",
    "typecheck": "turbo run typecheck --continue",
    "test": "turbo run test",
    "test:integration": "turbo run test:integration --concurrency=1",
    "ci": "pnpm lint:check && pnpm typecheck && pnpm test && pnpm test:integration",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "version-packages": "changeset version",
    "release:tag": "turbo run build --filter=@modulus-learning/agent && changeset tag",
    "release:npm": "turbo run build --filter=@modulus-learning/agent && changeset publish",
    "prepare": "simple-git-hooks || true"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm exec lint-staged"
  },
  "lint-staged": {
    "*.{js,jsx,mjs,cjs,ts,tsx,json,jsonc}": "biome check --write --no-errors-on-unmatched --diagnostic-level=error"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@changesets/cli": "^2.31.0",
    "lint-staged": "^17.0.8",
    "simple-git-hooks": "^2.13.1",
    "turbo": "^2.10.3",
    "typescript": "6.0.3"
  },
  "engines": {
    "node": ">=22.13"
  }
}
