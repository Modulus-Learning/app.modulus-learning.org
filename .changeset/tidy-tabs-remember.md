---
"@modulus-learning/agent": minor
---

Added scope-bound agent authentication, stable per-tab activity context across OAuth redirects, last-successful context restoration for cold tabs, same-page authentication single-flight handling, and canonical scope identity and display metadata in authenticated status.
